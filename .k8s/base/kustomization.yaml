resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - ingress.yaml
  - podDisruption.yaml

commonLabels:
  app.kubernetes.io/name: movie-catalog-service

commonAnnotations:
  app: movie-catalog-service

namePrefix: 
  kustom-

nameSuffix:
  -v1